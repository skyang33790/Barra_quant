# AGENTS.md

## 项目目标

本项目面向中国 A 股市场，目标是构建一个量化研究与模拟交易平台。使用tsffids这个python环境。

主要方向包括：

- 横截面选股
- 多因子 Alpha 研究
- IC / RankIC / 分层回测
- Barra 风格因子暴露分析
- 组合构建
- 历史回测

项目优先保证：

- 研究结论正确
- 数据口径清晰
- 工程结构简单
- 代码容易检查和维护

---

## 核心工程规则

- 修改代码前，先阅读并理解相关现有实现。
- 优先采用最小正确改动。
- 未经明确要求，不重构与当前任务无关的代码。
- 不为了“架构更优雅”而主动增加抽象层。
- 不轻易增加新的框架、服务、依赖或复杂目录结构。
- 优先沿用项目中已有的代码模式和约定。
- 如果改动涉及多个模块或明显改变架构，先说明方案，再开始实现。
- 如果需求存在会影响实现结果的重要歧义，先询问，不要自行做重大假设。
- 不要因为发现“顺手可以优化”的地方而扩大任务范围。

---

## 量化研究安全规则

凡是涉及以下内容：

- 行情数据
- 因子
- 标签
- 模型
- 股票池
- 组合
- 回测
- 模拟交易

都必须重点检查：

- 前视偏差（Look-ahead Bias）
- 数据泄漏（Data Leakage）
- 未来信息泄漏
- 时间戳错位
- 训练 / 验证 / 测试集污染
- 幸存者偏差
- 退市偏差
- 复权处理错误
- 公司行为处理错误
- 缺失值导致的信息泄漏
- 横截面数据泄漏
- 使用未来信息构造股票池

不要默认任何数据天然满足 Point-in-Time 要求。

对于每一个特征、因子、标签，都要明确：

- 这个数据是什么时候产生的
- 什么时候可以被策略看到
- 属于哪个交易日
- 策略最早什么时候可以使用它
- 对应的成交发生在什么时候

---

## 时间对齐规则

时间对齐属于最高优先级正确性问题。

必须明确区分：

- 数据时间
- 信息发布时间
- 因子计算时间
- 信号生成时间
- 下单时间
- 成交时间
- 持有周期
- 标签预测周期

除非策略设计明确保证数据在成交前已经可获得，否则：

- 不允许使用当日收盘价计算信号
- 再假设以同一个收盘价成交

如果执行时点存在不确定性，优先采用更保守的假设。

---

## A 股市场规则

回测和模拟交易在相关场景下应考虑：

- T+1
- 涨停限制
- 跌停限制
- 停牌
- ST / *ST
- 新股上市
- 退市股票
- 一手 100 股交易单位
- 交易手续费
- 印花税
- 过户费
- 滑点
- 流动性限制
- 公司行为
- 分红送转
- 交易日历

不要默认所有订单都可以立即、完全成交。

---

## 股票池规则

股票池必须避免未来信息污染。

在构建某个历史时点的股票池时，应只使用当时已经可获得的信息。

需要明确处理：

- 上市日期
- 退市日期
- ST 状态
- 停牌状态
- 上市不足 N 日
- 流动性过滤
- 市值过滤
- 行业过滤

不要直接使用“当前仍然上市的股票列表”去回测历史时期。

---

## 因子研究规则

对于 Alpha 因子，应优先检查：

- IC
- Rank IC
- ICIR
- 分层收益
- 多空收益
- 因子衰减
- 换手率
- 覆盖率
- 缺失率
- 极值情况
- 因子稳定性
- 行业暴露
- 市值暴露
- 与已有因子的相关性

不要只因为单次历史回测收益高，就认为因子有效。

因子必须尽量在多个时间区间、不同市场环境下验证。

---

## Barra 风格暴露

模型、Alpha 因子和投资组合应尽量分析其 Barra 风格暴露。

重点关注：

- 市值
- Beta
- 动量
- 波动率
- 非线性市值
- 价值
- 流动性
- 盈利
- 成长
- 杠杆

如果模型收益主要来自明显的风格暴露，应明确指出。

不要把已知风格收益误认为模型独立 Alpha。

---

## 机器学习规则

涉及机器学习时：

- 不允许随机打乱时间序列后直接划分训练集和测试集。
- 优先使用时间切分。
- 必要时使用 Walk-Forward 验证。
- 特征标准化、归一化、缺失值处理等步骤只能使用训练期信息拟合。
- 禁止使用测试集辅助选择特征或调参。
- 禁止根据最终测试结果反复调整模型。
- 所有预测结果应保存对应时间和股票代码，便于回溯。

模型评价不能只看收益。

至少应考虑：

- IC
- Rank IC
- ICIR
- 收益
- Sharpe
- 最大回撤
- 换手率
- 风格暴露
- 分阶段稳定性

---

## 回测规则

回测必须明确：

- 初始资金
- 股票池
- 调仓频率
- 信号时间
- 成交时间
- 成交价格
- 手续费
- 印花税
- 滑点
- 仓位限制
- 单股权重限制
- 是否允许现金
- 无法成交时如何处理

禁止使用不现实的“无限流动性、零成本、全部成交”作为最终结果。

简化假设可以存在，但必须明确标注。

---

## Paper Trading 规则

模拟交易应尽量复用未来实盘的接口设计。

建议通过统一 Broker 接口隔离：

- 策略
- 下单
- 持仓
- 资金
- 成交

例如：

- PaperBroker：模拟成交
- RealBroker：未来真实券商接口

策略层不要依赖具体 Broker 实现。

模拟盘至少记录：

- 信号
- 订单
- 成交
- 持仓
- 资金
- 手续费
- 拒单原因
- 未成交原因

---

## 数据层规则

数据获取层与研究层应尽量解耦。

上层策略不要直接依赖某一个具体数据源。

建议统一转换为内部标准格式，再供研究模块使用。

数据处理时应保留：

- 原始数据
- 清洗后数据
- 数据来源
- 更新时间
- 字段含义
- 复权方式
- 数据版本

不要静默修改历史数据。

---

## Debugging 规则

遇到 Bug 时：

- 先复现。
- 先定位根因。
- 先观察关键中间数据。
- 不要连续进行猜测式修改。
- 每次修改只解决一个明确问题。
- 修复后重新验证原问题。
- 必要时补充回归测试。

如果结果异常，优先检查：

- 时间对齐
- 数据范围
- 股票代码映射
- 复权
- NaN
- 排序方向
- 分组逻辑
- 收益计算
- 成交时点

---

## 测试规则

代码修改后，应优先运行与当前改动最相关的测试。

量化模块测试应尽量包含：

- 正常样本
- 边界情况
- 空数据
- NaN
- 停牌
- 涨跌停
- 上市 / 退市
- 时间边界
- 单股票
- 单交易日

不要为了让测试通过而删除或弱化已有有效断言。

---

## 修改后的验证

完成代码修改后：

1. 说明修改了什么。
2. 说明为什么这样修改。
3. 运行相关测试或验证命令。
4. 报告测试结果。
5. 如果没有验证，明确说明没有验证的原因。
6. 如果存在量化假设，明确列出这些假设。

不要只说“已完成”。

---

## 优先级

发生冲突时，按以下优先级判断：

1. 数据和研究正确性
2. 防止前视偏差和数据泄漏
3. 可验证性
4. 最小改动
5. 可维护性
6. 性能优化
7. 架构美观

除非已有明确性能瓶颈，否则不要为了性能提前增加复杂度。